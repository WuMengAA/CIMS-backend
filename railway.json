{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "uv sync --no-dev"
  },
  "deploy": {
    "startCommand": "uv run cims",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}
